1. User need to see a page report their submit
- If report has not been resolve by admin then user can delete
- If report has been resolve by admin then user can only view the report

2. Admin need to see a report has been resolve by them (can undo action if resolve mistake)

3. User can view a list of edit version of a comment
- User can edit comment infinite

4. Fix admin ui dashboard table to view on mobile

5. Finish dashboard page (current is empty)
- Implement word of a day to dashboard

6. Implement breadcrum, example: Home -> Quiz...

7. Implement review quiz after play and user can report question
- When play quiz user can also report question in middle term
- When review quiz user can also see comment
- When review quiz user can also report comment/report quiz

8. Implement email feature/password reset feature

9. http://localhost:3002/quiz -> Pagination, current view infinite quiz
-  Duplicate Edit Delete buttons -> Change cursor

10. Fix this error
ant-design-vue.js?v=db3f21d7:2765 Warning: [ant-design-vue: Modal] `visible` will be removed in next major version, please use `open` instead.

index.ts:161 [Vue Router warn]: The `next()` callback in navigation guards is deprecated. Return the value instead of calling `next(value)`.
(anonymous)	@	index.ts:161

11. Fix button + icon antd not align
<button data-v-be0bb461="" data-testid="add-question-button" class="css-dev-only-do-not-override-cz1kq2 ant-btn ant-btn-primary ant-btn-sm" type="button"><!----><span data-v-be0bb461="" role="img" aria-label="plus" class="anticon anticon-plus"><svg focusable="false" data-icon="plus" width="1em" height="1em" fill="currentColor" aria-hidden="true" viewBox="64 64 896 896"><path d="M482 152h60q8 0 8 8v704q0 8-8 8h-60q-8 0-8-8V160q0-8 8-8z"></path><path d="M192 474h672q8 0 8 8v60q0 8-8 8H160q-8 0-8-8v-60q0-8 8-8z"></path></svg><!----></span><span>Add Question</span></button>

12. Quiz edit page http://localhost:3002/quiz/368
- Allow to edit note
- Can view source from quiz edit page (open a modal or go to reading source page)

13. Can't attempt quiz that has no questions

14. Abstract a tag select component to fetch all existing tag of application and also can input tag directly (tag1, tag2, tag3...)

15. http://localhost:3002/questions implement pagination (current show infinite)

16. Implement add link source (parse html) and add file source (txt and pdf parse)

17. http://localhost:3002/search Implement pagination (current show infinite)

18. Implement avatar

19. Implement setting (change theme dark/light and language)

20. Implement forget password page (email)

21. Implement llm cache vs tts cache (so in small vps machine then maybe it can't run docker llm because not enough ram or too slow)
- Local dev machine create some llm/tts data an push to vps machine so atleast it can get cache result
- Calling llm/tts error is predicted so vps work find

22. Implement a health simple service to view all other service status UI/UX on vps

23. Implement a http api log to view metrics of system